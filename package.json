{
  "name": "production-mev-bot",
  "version": "3.0.0",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "deploy": "hardhat run scripts/deploy.ts --network mainnet"
  },
  "dependencies": {
    // 1. Revert to a current, compatible Ethers version (e.g., ^6.9.0)
    "ethers": "^6.9.0", 
    // 2. Update Flashbots constraint to allow for new versions that support newer Ethers
    "@flashbots/ethers-provider-bundle": "^1.1.0", 
    "winston": "^3.11.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "typescript": "latest",
    "@types/node": "latest",
    "hardhat": "latest",
    "@nomicfoundation/hardhat-toolbox": "latest"
  }
}
